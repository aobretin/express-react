import styled from "styled-components";
import Toolbar from '@material-ui/core/Toolbar';

export const HeaderToolbarStyles = styled(Toolbar)`
    display: flex;
    justify-content: space-between;
`;

export const NotesAndFilterStyles = styled.div`
    display: flex;
`;